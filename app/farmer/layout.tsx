import type { ReactNode } from 'react'

/** Shell is provided by `AuthenticatedAppShell` in `SessionGate`. */
export default function FarmerLayout({ children }: { children: ReactNode }) {
  return children
}
